# Blue Coast Sistema Hotelero

Paquete listo para GitHub Pages.

Subir el contenido de esta carpeta como raiz del repositorio. No subir la carpeta `datos` del sistema local.

La conexion con Google Sheets se configura en:

```text
config.js
```

La pagina principal es:

```text
index.html
```
